/**
 * Client-safe constants for period selection. No server-only imports here so
 * that client components (e.g. PeriodSelector) can import it freely.
 */
export const PERIOD_COOKIE = "selected_period_id";
