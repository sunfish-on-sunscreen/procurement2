# ARCHITECTURE MAP — 00: MECHANICAL INVENTORY (Phase A)

> Generated by a read-only documentation pass on **2026-07-15** at HEAD `a99a0c0`.
> This file holds the **denominators** every later section must cover 100% of.
> Companion files: `ARCHITECTURE_MAP_01`…`_07`. Nothing here or in the companions
> was committed or staged — all `ARCHITECTURE_MAP_*.md` are left **untracked**.
>
> **Evidence rule for the whole map:** every behavioural claim cites `path:line` and
> quotes the actual source. Counts below are produced by commands (shown), not memory.

---

## A1 — FILES (denominator = **183** source files)

Command (Git Bash):
```
find app lib components hooks types python prisma scripts \
  \( -name "*.ts" -o -name "*.tsx" -o -name "*.py" -o -name "*.prisma" \
     -o -name "*.sql" -o -name "*.js" -o -name "*.mjs" -o -name "*.css" \) -type f \
  | grep -viE "lib/generated|python/\.venv|python/__pycache__|node_modules"
# + root: proxy.ts next.config.ts prisma.config.ts eslint.config.mjs postcss.config.mjs
```
- `hooks/` and `types/` directories **do not exist** (0 files) — shared types live in
  `lib/*-types.ts`; there are no custom React hooks outside `lib/use-table-sort.ts` and
  `components/charts/useAnimatedDomain.ts`.
- **Excluded from the 183** (generated / vendored / non-source): `lib/generated/prisma/**`
  (Prisma client codegen), `python/.venv/**` (4343 venv files), `python/__pycache__/**`,
  `node_modules/**`, `.next/**`, `public/**` (static assets), and pure JSON configs
  (`package.json`, `tsconfig.json`, `components.json`, `package-lock.json`) — these are
  documented narratively in §0 where relevant but are **not** in the A1 count.

### A1 ownership partition (each file mapped to exactly one companion doc; union = 183, intersection = ∅)

| Owner doc | Cluster | Files |
|---|---|---:|
| `_01_TOPOLOGY_DATA` | Repo topology, data layer, auth, period/range plumbing, migrations | 38 |
| `_02_FOUNDATION` | Design system: globals.css tokens, `components/ui/*`, shared shells, chart infra, format libs | 36 |
| `_03_PAGES_SPEND_CLASS` | Spend Overview + Supplier Classification pages | 34 |
| `_04_PAGES_PROCESS_ACTION` | Process Health + Action Priorities pages | 25 |
| `_05_PAGES_IMPORT_REPORTS_METH` | Import + Reports + Methodology pages + supplier/purchase CRUD | 39 |
| `_06_COMPUTE_ANOMALY` | Python compute, anomaly libs, breakdown, offline transformer | 11 |
| **TOTAL** | | **183** |

The exact per-owner file list is in `scratchpad/A1_tagged.txt` and reproduced at the head
of each companion doc. The Phase-C coverage proof (`_07`) checks each doc's documented-file
count against its assignment.

---

## A2 — DB FIELDS (denominator = **97 scalar fields + 18 relation fields across 10 models + 2 enums**)

Source of truth: `prisma/schema.prisma` (221 lines, single file). Field counts per model
(scalar / relation), read directly from the schema:

| Model | Scalar fields | Relation fields | Key constraints/indexes |
|---|---:|---:|---|
| `User` | 6 | 3 | `@@unique` none extra; `email @unique` |
| `Session` | 4 | 1 | FK `userId`→User `onDelete: Cascade` |
| `ReportingPeriod` | 6 | 6 | `name @unique` |
| `Supplier` | 6 | 1 | `@@unique([externalId, periodId])`, `@@index([periodId])`, `@@index([category])` |
| `Purchase` | 25 | 1 | `@@unique([poId, periodId])`, `@@index([periodId])`, `@@index([supplierExternalId, periodId])` |
| `SupplierMetric` | 20 | 1 | `@@unique([supplierExternalId, periodId])`, `@@index([periodId])` |
| `Import` | 10 | 2 | `@@index([periodId])`, `@@index([uploadedAt])` |
| `AnalysisResult` | 7 | 1 | `@@unique([periodId, analysisType])`, `@@unique([rangeStartDate, rangeEndDate, analysisType])`, 2 indexes |
| `ExecutiveSummary` | 7 | 2 | `@@index([periodId])` |
| `ReportPreset` | 6 | 0 | `@@index([createdBy])` — **orphaned** (no code path reads/writes it; confirmed in _01) |
| **TOTAL** | **97** | **18** | |

Enums (2): `Role { ADMIN, VIEWER }` (schema:28-31); `ImportStatus { PENDING, PROCESSING, SUCCESS, FAILED }` (schema:158-163).

Full field-by-field walk (type + purpose + which page consumes it) is in `_01` §1. Every one
of the 97 scalar + 18 relation fields gets a row there.

---

## A3 — EXPORTS (denominator = **382** `export` declarations in .ts/.tsx + **63** Python top-level symbols)

Command: count of `(?m)^export\s` per file, summed by owner (robust — captures
`export const/function/async function/class/type/interface/enum/default/{ … }`):

| Owner doc | `export` decls |
|---|---:|
| `_01_TOPOLOGY_DATA` | 72 |
| `_02_FOUNDATION` | 56 |
| `_03_PAGES_SPEND_CLASS` | 56 |
| `_04_PAGES_PROCESS_ACTION` | 62 |
| `_05_PAGES_IMPORT_REPORTS_METH` | 108 |
| `_06_COMPUTE_ANOMALY` (TS anomaly libs only) | 28 |
| **TOTAL (TS/TSX)** | **382** |

Python top-level symbols (`^def ` + `^UPPER_CONST =`), documented in `_06`:
`scores.py` 16, `compute_analyses.py` 34, `import_compute.py` 3, `transform_dataset.py` 10 = **63**.

> Note: an earlier looser regex counted 362; the 382 figure (used as the A3 denominator) is
> the robust count including `export {` re-exports and `export type`. Each companion doc
> enumerates its files' exports and reports its documented count; `_07` sums them vs 382.

---

## A4 — THRESHOLDS & NUMERIC CONSTANTS (named constants confirmed so far; full enumeration in `_06`)

Named threshold constants found by `grep -nE '\b[A-Z][A-Z0-9_]{2,}\b\s*[:=]\s*-?[0-9]'`:

| Constant | Value | Location |
|---|---|---|
| `RECOMPUTE_PERIOD_TIMEOUT_MS` | `60_000` | `lib/recompute.ts:6` |
| `STAGE_FLAG_DAYS` | `8` | `lib/report-narrative.ts:186` |
| `TRAJECTORY_PARTIAL_FRACTION` | `0.5` | `lib/report-narrative.ts:1144` |
| `SPEND_FOLD_CUTOFF` | `2.5` | `lib/temporal-anomalies.ts:33` |
| `SPEND_SMALL_BASE_MIN` | `100_000` | `lib/temporal-anomalies.ts:35` |
| `SCORE_SWING_CUTOFF` | `18` | `lib/temporal-anomalies.ts:37` |
| `PARTIAL_YEAR_SPEND_FRACTION` | `0.5` | `lib/temporal-anomalies.ts:40` |
| `CLASSIFICATION_DISAGREEMENT_CUTOFF` | `80` | `lib/anomaly-crossref.ts:190` |
| `_CONC_POINTS` | `{0:50, 1:35, 2:22, 3:12, 4:5}` | `python/scores.py:78` |
| `MS_PER_DAY` | `86_400_000` | `lib/purchase-import.ts:24` |
| `CAT2_LARGE` | `15` | `lib/report-templates.ts:312` |
| Inconsistent-flag cutoff | `1.5 × median(IQRs)` | `lib/cycle-flags.ts:67` |
| Outlier PO cutoff | `> 2σ` above period mean | `python/compute_analyses.py` (z-score emitter) |
| Stage-dominated cutoff | one stage `> 60%` of total cycle | `python/compute_analyses.py` (stage anomaly) |
| ABC thresholds | `80% / 95%` cumulative | `python/compute_analyses.py` (abc emitter) |
| Category-concentration threshold | `0.30` | `python/compute_analyses.py` (recommendations) |
| Tail-spend share | `0.01` | `python/compute_analyses.py` (recommendations) |
| Composite weights | `0.30·Q + 0.30·D + 0.22·P + 0.18·R` | `python/scores.py` |

The **complete** file:line enumeration of every numeric literal driving a metric — including
every `norm_high/norm_low` bound, the `country_distance` tiers, the Kraljic risk-component
caps (50/25/25), the median-split operators (`>` vs `>=`), and the perf-zone quadrant cutoffs
— is produced in `_06` (compute substrate). `_07` proves that every metric in A5 resolves to
a constant documented there.

---

## A5 — METRICS / KPIs / ANOMALIES RENDERED (enumerated per-page in §3; totaled in `_07` §6)

A5 is the set of **distinct computed values the UI displays** (not static copy). Because a
metric's denominator only means something against the page that renders it, A5 is enumerated
as a numbered sub-list at the head of each page's §3c/§3d in the companion docs, then summed
in `_07`. The high-level families (each expanded to individual values in the page docs):

1. Spend Overview — KPI row (total spend, invoices, active suppliers, avg invoice), monthly
   trend series, spend-by-category donut, top-10 bars, Pareto/ABC (class A/B/C counts + spend
   + cumulative %), 54-row ranking (spend/invoices/avg/ABC per row), InsightsPanel computed
   sentences, `total_categories`.
2. Supplier Classification — Kraljic scatter (quadrant assignment + supply-risk axis),
   performance-vs-spend scatter (zone assignment), quadrant/zone count+avg tables, synthesis
   cards, KPI StatBlocks.
3. Process Health — cycle median/IQR/mean/range, box plot + outliers, stage decomposition
   (4 stage means + %), stage occupancy series, 3-way-match / control-exposure $, roster
   (median/POs/slowest-stage/anomalies per supplier), 3 anomaly flag counts (**14 / 2 / 35**).
4. Action Priorities — glance narrative, StatBlock grid, "Where to act" group cards, 8 rec
   categories, cross-analysis anomaly hub (**46 / 36 / 11 / 18**, Important 17, In-2+ 19),
   ActionInsightCard tabbed panels.
5. Reports — the argument (headline/situation/findings/actions/watching), supplier brief,
   category deep-dive, appendix analysis tables, 3-family anomaly summary.
6. Methodology / Import — mostly static copy + form state (few computed values; enumerated in `_05`).

---

## A6 — QUERIES (denominator = **56** Prisma call-sites in `app/` + **19** in real `lib/` + Python raw SQL, enumerated in `_06`)

Command: `grep -rnE 'prisma\.\w+\.(findMany|findFirst|findUnique|create|createMany|update|updateMany|upsert|delete|deleteMany|aggregate|groupBy|count)|\$transaction|\$queryRaw|\$executeRaw'`

- **`app/` — 56 occurrences across 22 files** (route handlers + server pages). Per-file counts
  captured in scratchpad; each is documented in the owning page doc's §3b.
- **`lib/` (non-generated) — 19 occurrences across 9 files**: `period.ts` 1, `cycle-breakdown.ts` 1,
  `auth.ts` 3, `recompute.ts` 2, `analysis-types.ts` 1, `report-focus.ts` 4, `range-analyses.ts` 2,
  `temporal-load.ts` 1, `suppliers.ts` 4. (The 241 hits inside `lib/generated/prisma/**` are
  codegen boilerplate — excluded.)
- **Python raw SQL** — `compute_analyses.py` reads via a `_df(conn, 'SELECT …')` helper
  (`SELECT * FROM "Purchase"`, `DISTINCT ON` reads of `Supplier`/`SupplierMetric`,
  `ReportingPeriod` bounds) and writes `AnalysisResult` via `cur.execute` upserts;
  `import_compute.py` reads raw rows. Full file:line list in `_06` §A6-python.

`_07` §6 lists all 75 TS query-sites + the Python SQL statements against their documented
entries with a per-owner delta.
